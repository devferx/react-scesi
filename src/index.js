import React from "react";
import ReactDOM from "react-dom";
// import App from "./App";
import Router from "./Router/index";

const contenedor = document.getElementById("root");

ReactDOM.render(<Router />, contenedor);
